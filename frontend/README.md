# Frontend

The React/TypeScript single-page app (ADR-005). Bounded here, scaffolded with
Vite when the first UI slice lands (scaffolding-plan Phase B.7 / D).

Empty by design for now — the backend walking skeleton (Phase B) comes first.
